# Local project state — generated when feature-compass dogfoods itself.
# These artifacts are specific to this project's own ship history, not part
# of the skill template shipped to users.
feature-compass/
